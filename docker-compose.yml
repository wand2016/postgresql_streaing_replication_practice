version: "3"
services:
  master:
    image: postgres
    tty: true
    volumes:
      - "./docker/master/docker-entrypoint-initdb.d:/docker-entrypoint-initdb.d"
  standby1:
    image: postgres
    tty: true
  standby2:
    image: postgres
    tty: true
